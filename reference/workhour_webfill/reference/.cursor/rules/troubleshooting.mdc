---
description: 
globs: 
alwaysApply: true
---
# 问题排查指南

## 当前已知问题

### TypeScript类型错误

#### Button组件属性错误
**问题**：[app/page.tsx](mdc:app/page.tsx)中多处Button组件类型错误
```
Property 'variant' does not exist on type 'IntrinsicAttributes & ButtonProps'
Property 'size' does not exist on type 'IntrinsicAttributes & ButtonProps'
```

**原因**：[components/ui/button.tsx](mdc:components/ui/button.tsx)的ButtonProps接口可能未定义variant和size属性

**解决方案**：
1. 检查Button组件的类型定义
2. 确保ButtonProps接口包含variant和size属性
3. 或者移除使用这些属性的代码

#### Lucide React图标问题
**问题**：`Cannot find module 'lucide-react'`

**解决方案**：
```bash
npm install lucide-react
# 或
npm install lucide-react@latest
```

## 常见功能问题

### Excel生成失败
**可能原因**：
1. 工作模板为空（至少需要一个RandomWorkItem）
2. XLSX库未正确安装
3. 浏览器不支持下载功能

**排查步骤**：
1. 检查`randomWorkItems.length > 0`
2. 验证Excel数据格式是否正确
3. 检查文件下载逻辑

### 配置导入失败
**可能原因**：
1. JSON格式错误
2. 字段名称不匹配
3. 数据类型错误

**排查方法**：
```typescript
try {
  const jsonData = JSON.parse(e.target?.result as string);
  // 验证数据结构
} catch (error) {
  console.error('JSON解析失败:', error);
}
```

### 日历显示异常
**可能原因**：
1. 日期计算错误
2. 星期计算逻辑问题
3. 月份边界处理错误

**检查点**：
- `getDaysInMonth()`函数的日期计算
- 周末讲堂状态检查
- 月份切换时的年份处理

## 性能问题

### 组件重渲染
**现象**：页面响应缓慢，特别是表单操作时

**解决方案**：
1. 使用React.memo包装大型组件
2. 优化状态更新逻辑
3. 考虑使用useCallback和useMemo

### 大量数据处理
**现象**：生成Excel时页面卡顿

**优化方向**：
1. 使用Web Workers处理大量数据
2. 分批处理数据
3. 添加进度指示器

## 调试技巧

### 状态调试
在组件中添加调试信息：
```typescript
useEffect(() => {
  console.log('Current state:', {
    randomWorkItems: randomWorkItems.length,
    fixedWorkItems: fixedWorkItems.length,
    weekendLectures: weekendLectures.length
  });
}, [randomWorkItems, fixedWorkItems, weekendLectures]);
```

### Excel数据验证
在生成前检查数据：
```typescript
console.log('Excel data preview:', data.slice(0, 5));
console.log('Total rows:', data.length);
```

### 时间计算验证
```typescript
const debugTimeCalculation = (hour: number, minute: number, schedule: ScheduleConfig) => {
  const timeValue = hour + minute / 60;
  const startValue = schedule.startHour + schedule.startMinute / 60;
  const endValue = schedule.endHour + schedule.endMinute / 60;
  console.log('Time check:', { timeValue, startValue, endValue, inRange: timeValue >= startValue && timeValue < endValue });
};
```

## 部署问题

### 构建失败
**常见原因**：
1. TypeScript类型错误
2. 依赖缺失
3. 环境变量问题

**解决步骤**：
```bash
# 清理依赖
rm -rf node_modules package-lock.json
npm install

# 类型检查
npm run build
```

### 运行时错误
**检查项**：
1. 浏览器兼容性（特别是文件下载API）
2. CORS问题（如果有外部API调用）
3. 内存限制（大文件处理时）

